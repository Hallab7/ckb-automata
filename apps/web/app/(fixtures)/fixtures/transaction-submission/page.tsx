import { CreationSubmissionFixture } from "../../../../src/setup/creation-submission.fixture.tsx";

export default function TransactionSubmissionFixturePage() {
  return <CreationSubmissionFixture />;
}
